version: '3.9'

services:

  project-db:
    image: postgres:17.4
    restart: always
    container_name: project-postgres-db
    env_file: project.env
    volumes:
      - ./var/lib/postgresql/data
    ports:
      - "5432:5432"
