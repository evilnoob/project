group 'ru.evilnoob.project.auth'
version rootProject.version

dependencies {
    compile project(":project-common")

    compile "org.springframework.boot:spring-boot-starter-data-jpa"
    compileOnly "org.projectlombok:lombok:$lombokVersion"
}
